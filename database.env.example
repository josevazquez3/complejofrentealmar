# Copiá a .env.local (y, si usás npx prisma sin npm run, también a .env en la raíz).
# Reemplazá USER, PASSWORD y host por Neon → Connection details → connection string (Pooled).
#
# DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxxx.region.aws.neon.tech/neondb?sslmode=require
